import 'package:flutter/material.dart';

class MapsHistory extends StatelessWidget {
  const MapsHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mapas Hsitory'),
    );
  }
}
